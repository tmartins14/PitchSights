---

## `experimentation/README.md`

```md
# experimentation

Notebooks and ad-hoc scripts. Nothing here runs in prod.

## Structure

- `notebooks/` — exploratory analysis, plots
- `sweeps/` — threshold and hyperparameter sweeps
- `reports/` — static markdown write-ups

## Tip

Pin notebook dependencies via `requirements-dev.txt` and avoid importing from `production/*`.
```
